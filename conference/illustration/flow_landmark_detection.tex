\documentclass[tikz, border=10pt]{standalone}
\usepackage{tikz}
\usetikzlibrary{shapes.geometric, arrows.meta, positioning}

% ── styles ──────────────────────────────────────────────────────────────────
\tikzset{
  io/.style={
    trapezium, trapezium left angle=75, trapezium right angle=105,
    draw, thick, fill=black!10,
    text centered, minimum width=3.8cm, minimum height=0.7cm,
    font=\small
  },
  process/.style={
    rectangle, draw, thick, fill=white,
    text centered, minimum width=3.8cm, minimum height=0.7cm,
    font=\small
  },
  model/.style={
    rectangle, draw, very thick, fill=black!20,
    text centered, minimum width=3.8cm, minimum height=0.8cm,
    font=\small\bfseries
  },
  decision/.style={
    diamond, draw, thick, fill=black!5,
    text centered, minimum width=2.4cm, minimum height=0.7cm,
    inner sep=0pt, font=\scriptsize
  },
  mapping/.style={
    rectangle, draw, thick, fill=black!8,
    text centered, minimum width=3.8cm, minimum height=1.6cm,
    font=\scriptsize
  },
  discard/.style={
    rectangle, draw, thick, dashed, fill=black!5,
    text centered, minimum width=2.2cm, minimum height=0.6cm,
    font=\scriptsize
  },
  output/.style={
    rectangle, draw, very thick, fill=black!30, text=white,
    text centered, minimum width=3.8cm, minimum height=0.8cm,
    font=\small\bfseries
  },
  arr/.style={-{Stealth[length=6pt]}, thick},
  darr/.style={-{Stealth[length=5pt]}, thick, dashed},
}

\begin{document}
\begin{tikzpicture}[node distance=0.75cm and 2.4cm]

  % ── nodes ─────────────────────────────────────────────────────────────────
  \node[io]       (input)   {Input Image (640$\times$640)};
  \node[model,    below=of input]   (pose)    {YOLOv8x-pose};
  \node[process,  below=of pose]    (persons) {Person Detections\\{[bbox, conf]}};
  \node[decision, below=of persons] (dconf)   {conf $<$ 0.50?};
  \node[process,  below=of dconf]   (kpts)    {17 Keypoints / person\\(x, y, visibility)};
  \node[decision, below=of kpts]    (dvis)    {vis $<$ 0.3?};
  \node[mapping,  below=of dvis]    (anchor)  {%
    Build Anchor Regions\\[4pt]
    \texttt{head kps} $\rightarrow$ helmet region\\
    \texttt{shoulder kps} $\rightarrow$ vest region\\
    \texttt{wrist kps} $\rightarrow$ gloves region
  };
  \node[output,   below=of anchor]  (out)     {Output: Skeletal Anchors\\{[person\_id, kpts, regions]}};

  % discard nodes
  \node[discard, right=of dconf]   (disc1)   {Discard\\person};
  \node[discard, right=of dvis]    (disc2)   {Suppress\\keypoint};

  % ── edges ─────────────────────────────────────────────────────────────────
  \draw[arr] (input)   -- (pose);
  \draw[arr] (pose)    -- (persons);
  \draw[arr] (persons) -- (dconf);
  \draw[arr] (dconf)   -- node[left, font=\scriptsize]{No}  (kpts);
  \draw[arr] (kpts)    -- (dvis);
  \draw[arr] (dvis)    -- node[left, font=\scriptsize]{No}  (anchor);
  \draw[arr] (anchor)  -- (out);

  \draw[darr](dconf)   -- node[above, font=\scriptsize]{Yes} (disc1);
  \draw[darr](dvis)    -- node[above, font=\scriptsize]{Yes} (disc2);

\end{tikzpicture}
\end{document}
